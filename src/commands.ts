/**
 * Command definitions for the VSCode extension.
 * 
 * Define all your extension commands here using the enum pattern.
 * Commands should follow the format: "extension-identifier.commandName"
 * 
 * Example:
 * export enum Command {
 *     HelloWorld = "your-extension-name.helloWorld",
 *     OpenSettings = "your-extension-name.openSettings",
 * }
 */
export enum Command {
    // Add your commands here following the pattern:
    // CommandName = "extension-identifier.commandName",
    
    // Example commands (uncomment and customize):
    // HelloWorld = "vscode-extension-template.helloWorld",
    // ShowOutput = "vscode-extension-template.showOutput",
}
